package com.depvt.live.football.tv.utils.interfaces

import androidx.navigation.NavDirections

////This interface is for controlling navigation between fragments
interface MoreScreenNavigate {
    fun moveTo(id: Int)
}